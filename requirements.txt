# Бот
aiogram>=3.31,<4
python-dotenv>=1.0,<2
# База часовых поясов IANA для zoneinfo. Ставим ВСЕГДА, а не только на Windows:
# у Windows своей базы нет никогда, а у Linux она бывает то есть, то нет —
# на полном Debian/Ubuntu есть, а на slim-образах (наш Docker) её тоже нет.
# Пакет tzdata безвреден, если системная база уже есть: zoneinfo сначала
# смотрит в систему и обращается к этому пакету только как к запасному.
tzdata>=2024.1

# Рендер "хронологии недели" (render/render_week.py) — используется со
# следующего шага; для скелета не нужен, но фиксируем версии сразу.
Jinja2>=3.0,<4
playwright>=1.40,<2

# Слой данных
SQLAlchemy[asyncio]>=2.0,<3
alembic>=1.13,<2
asyncpg>=0.29,<1

# Разбор голосовых через LLM (любой OpenAI-совместимый провайдер)
openai>=1.40,<2
# httpx идёт вместе с openai, но socks5-прокси он умеет только с этим
# дополнением — а именно socks отдаёт большинство VPN-клиентов
# (LLM_PROXY=socks5://...). Для http-прокси дополнение не нужно.
httpx[socks]>=0.27
