# Бот
aiogram>=3.31,<4
python-dotenv>=1.0,<2

# Рендер "хронологии недели" (render/render_week.py) — используется со
# следующего шага; для скелета не нужен, но фиксируем версии сразу.
Jinja2>=3.0,<4
playwright>=1.40,<2

# Слой данных
SQLAlchemy[asyncio]>=2.0,<3
alembic>=1.13,<2
asyncpg>=0.29,<1

# Разбор голосовых через LLM (любой OpenAI-совместимый провайдер)
openai>=1.40,<2
