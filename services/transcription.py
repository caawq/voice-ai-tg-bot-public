"""
Транскрипция голосового в текст.

Отдельный протокол от LLMClient (services/llm.py), хотя за обеими в проекте
сейчас стоит один и тот же провайдер: у Gemini нет отдельного эндпоинта вида
OpenAI Whisper (/audio/transcriptions) в OpenAI-совместимом слое — аудио
понимается только внутри обычного chat completions, через блок "input_audio"
в сообщении. Разделение на два протокола всё равно того стоит: это разные
задачи (голос -> текст vs текст -> структура), и провайдер транскрипции
завтра может смениться независимо от провайдера разбора (например, Whisper
у одного вендора и Gemini у другого) — на этот случай TRANSCRIBE_MODEL в
конфиге можно указать отдельно от LLM_MODEL.
"""

from __future__ import annotations

from typing import Protocol

TRANSCRIBE_SYSTEM_PROMPT = (
    "Расшифруй голосовое сообщение пользователя дословно, на языке оригинала. "
    "Верни только сам текст — без кавычек, без комментариев, без 'Пользователь "
    "сказал:'. Если разобрать ничего нельзя (тишина, шум, не найден язык) — "
    "верни пустую строку."
)


class TranscriptionError(RuntimeError):
    """Провайдер не смог расшифровать аудио: сеть, лимиты, не понял формат."""


class TranscriptionClient(Protocol):
    """Контракт транскрибатора. В тестах подменяется фейком, как и LLMClient."""

    async def transcribe(self, *, audio: bytes, mime_type: str) -> str:
        """Вернуть распознанный текст. Пустая строка — расшифровать нечего."""
        ...
